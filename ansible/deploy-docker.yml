---
- name: install docker
  hosts: docker
  become: yes
  tasks:
#    - name: Docker repo download
#      ansible.builtin.uri:
#        url:
    - name: Install docker
      ansible.builtin.yum:
          name: httpd>=2.4
          state: present