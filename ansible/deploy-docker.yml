---
- name: install docker
  hosts: docker
  become: yes
  tasks:
#    - name: Docker repo download
#      ansible.builtin.uri:
#        url:
    - name: Install docker
      yum:
        pkg:
          - docker
        update_cache: yes
        state: installed
